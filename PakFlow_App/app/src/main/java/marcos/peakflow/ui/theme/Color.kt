package marcos.peakflow.ui.theme

import androidx.compose.ui.graphics.Color

// --- Colores Principales ---
val RedPeakFlow = Color(0xFFEE4A4A)

// --- Paleta de Grises/Negros para el Modo Oscuro ---
val Black = Color(0xFF121212)       // Fondo principal, muy oscuro pero no negro puro
val Gray = Color(0xFF393939)       // Color secundario, para superficies como la TopAppBar
val DarkSurface = Color(0xFF424242) // Un gris ligeramente más claro para tarjetas y diálogos
val LightGray = Color(0xFFC2C2C2) // Texto secundario o iconos con menos énfasis


// --- Paleta de Blancos/Grises para el Modo Claro ---
val WhiteBackground = Color(0xFFF7F7F7) // Fondo principal, un blanco ligeramente roto
val WhiteSurface = Color(0xFF8B8B8B)    // Superficies como tarjetas, blanco puro para contraste
val DarkGrayText = Color(0xFF5A5A5A)  // Texto secundario o iconos en modo claro
val StrokeGray = Color(0xFFE0E0E0)   // Para bordes o divisores sutiles

// --- Colores de Acento y Estado ---
val GreenSuccess = Color(0xFF4CAF50)    // Para mensajes de éxito
val OrangeWarning = Color(0xFFFF9800)  // Para advertencias
val RedError = Color(0xFFD32F2F)        // Un rojo específico para errores, ligeramente distinto al principal

// --- Colores para Métricas (opcional, pero recomendado) ---
val BlueMetric = Color(0xFF95D0E8)
val PurpleMetric = Color(0xFF9575CD)



//--- antiguos---

val backgroudNavSelectedItem = Color(0xFF666666)
val BackgroundButton = Color(0x00111111)
val ShapeButton = Color(0xFF8D8D8D)
